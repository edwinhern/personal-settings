#!/usr/bin/env bash
# Source-state hash: {{ include "dot_config/mise/config.toml.tmpl" | sha256sum }}
# Re-runs whenever the mise config template content changes.

set -euo pipefail

if ! command -v mise >/dev/null 2>&1; then
  echo "mise not on PATH; skipping mise install" >&2
  exit 0
fi

mise install
