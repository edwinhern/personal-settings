# APM compiled output and cache — generated by apm install
# apm.lock.yaml is checked in (pins dependency commits for reproducibility);
# run `apm install --update` then `chezmoi add ~/.apm/apm.lock.yaml` to refresh.
apm_modules/
.claude/
.agents/
.github/
